```cpp
x & (1 << (i - 1)) // x从左至右第i位是否为1
    
x = x | (1 << (i-1)) // 将x从左至右第i位置1

x = x & (x - 1) // 把二进制最靠右的第一个1去掉
    
x & -x // 高位1清空，只保留最低位的1
    
x - (x & -x) == x ^ (x & -x) // 减去最低位的1
```





